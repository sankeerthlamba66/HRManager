namespace HRManager.Utilities
{
    public static class ErrorLogger
    {
        public static int LogException(Exception ex)
        {
            //Log into database and return the id of last inserted record
            return 0;
        }

        public static int LogError(string Message)
        {
            //Log into database and return the id of last inserted record
            return 0;
        }

        public static int LogInfo(string Message)
        {
            //Log into database and return the id of last inserted record
            return 0;
        }
    }
}